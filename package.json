{
  "name": "liyueyue-locker-folio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "bootstrap": "node scripts/bootstrap.mjs",
    "dev": "node scripts/bootstrap.mjs && npm --prefix .template install && npm --prefix .template run dev -- --host 0.0.0.0",
    "build": "node scripts/bootstrap.mjs && npm --prefix .template install && npm --prefix .template run build",
    "preview": "npm --prefix .template run preview -- --host 0.0.0.0"
  }
}
